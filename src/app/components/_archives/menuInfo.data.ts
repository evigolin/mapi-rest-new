export interface menuInfo {
    name: string;
    redirectTo: string;
}

/** list of Menus Info */
export const menusInfo: menuInfo[] = [
    {
        name: "Help_Center",
        redirectTo: "/help"
    }
]